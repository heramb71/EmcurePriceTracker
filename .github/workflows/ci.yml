name: CI

# Runs the test suite on every push and PR to main. This is the gate that was
# missing: 233 tests existed but nothing ran them before a deploy shipped.
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          # Match the Oracle Cloud deploy target (Ubuntu 22.04 / Python 3.10) so
          # CI catches 3.10 incompatibilities before they reach the server — e.g.
          # a stdlib module (tomllib) that only exists on 3.11+.
          python-version: "3.10"
          cache: pip

      - name: Install core deps
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements-core.txt

      - name: Run tests
        # FINBERT_MODEL_PATH=skip keeps torch/transformers off the runner, same
        # as the server. Network calls in tests are mocked; nothing hits yfinance.
        env:
          FINBERT_MODEL_PATH: skip
        run: python -m pytest -q

      - name: Lint (ruff)
        run: |
          pip install ruff
          ruff check src apps tests
